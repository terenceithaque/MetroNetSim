MetroNetSim is a Python graphical program allowing to simulate metro networks created by the user. These networks are modelized as graphs and can be saved locally as database files (.db).

For example, the user might add lines, stations, etc. and determine the best available solutions.